return {
  {
    "mattn/emmet-vim",
    event = "VeryLazy",
    config = function()
      -- NerdCommenter specific setup or configuration commands go here.
      -- For NerdCommenter, you might not need any specific setup commands.
      -- However, if there are any recommended configurations or settings
      -- you'd like to use, you can add them here.
    end,
  },
}
